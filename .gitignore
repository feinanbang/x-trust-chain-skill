# Secrets — never commit your keys
.env

# Local config (each user creates their own from config.example.json)
config.json

# Dependencies
node_modules/

# Generated output and cache
output/
.trust-chain/
*-cache.json
